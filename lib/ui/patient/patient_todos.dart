import 'package:flutter/material.dart';

class PatientTodos extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("Patient Todos"),
    );
  }
}
